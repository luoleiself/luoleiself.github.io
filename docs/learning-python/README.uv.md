# Wheel

一种二进制分发包格式, 本质上是一个包含预编译代码和元数据的 zip 压缩包。 它不是可执行程序, 而是用于库和工具的分支。 Wheel
包在安装时, pip 会自动将 .py 源代码编译为 .pyc 字节码, 并生成入口点脚本。

# tool

uv 专门用来管理全局命令行工具的子命令, 核心作用是将 python 包作为全局工具安装, 并将其隔离在独立的虚拟环境中,
避免污染当前项目的依赖或系统的全局 python 环境。

- uv tool install <package> # 安装后可以在电脑的任意目录下直接运行该工具的命令, 而无需激活任何虚拟环境。
